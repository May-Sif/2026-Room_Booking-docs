# System Architecture

## High-Level Architecture

Client (React + TypeScript)
        ↓ REST API
ASP.NET Web API
        ↓
SQLite Database

## Description

The frontend communicates with the backend through RESTful API endpoints.
The backend handles business logic, validation, and database operations.
